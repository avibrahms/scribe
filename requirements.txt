# Scribe — cross-platform dependencies.
#
# These are the deps used by scribe_core.py and both the macOS and Windows
# entry points. Platform-specific deps live in:
#   • requirements-mac.txt
#   • requirements-windows.txt
#
# Install:
#   macOS   — pip install -r requirements.txt -r requirements-mac.txt
#   Windows — pip install -r requirements.txt -r requirements-windows.txt

edge-tts>=7.2.8
sounddevice>=0.4.7
numpy>=1.26,<3
httpx>=0.27
