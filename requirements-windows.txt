# Scribe — Windows-only dependencies.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-windows.txt

pystray>=0.19.5
Pillow>=10
pynput>=1.7.7
pyperclip>=1.8.2
# playsound 1.3.0 has a Windows bug with spaces in paths; 1.2.2 is stable.
playsound==1.2.2
